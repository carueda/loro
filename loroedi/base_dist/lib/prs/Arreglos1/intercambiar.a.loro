algoritmo para intercambiar(arreglo: []entero, i, j: entero)
estrategia ''Con ayuda de una variable temporal.''
inicio
	temp: entero;
	temp := arreglo[i];
	arreglo[i] := arreglo[j];
	arreglo[j] := temp;
fin algoritmo
