algoritmo para intercambiar(arreglo: []entero, i, j: entero)
''Nos ayudamos de una variable temporal.''
inicio
	temp: entero;
	temp := arreglo[i];
	arreglo[i] := arreglo[j];
	arreglo[j] := temp;
fin algoritmo
