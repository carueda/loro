// demostracion de intercambio en arreglo.
a: []entero := [ 0, 10, 20, 30 ];
escribirln("arreglo antes   = " +a);

escribirln("intercambiando posiciones 1 y 3...");
intercambiar(a, 1, 3);

escribirln("arreglo despues = " +a);
