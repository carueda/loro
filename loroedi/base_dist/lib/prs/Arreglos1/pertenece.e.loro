especificación pertenece(a: []entero constante, n: entero) 
		-> esta: booleano
	''Este proceso informa si un número está en 
	un arreglo de números.''
	entrada
		a : ''El arreglo de números.''
		n : ''El número a buscar en el arreglo.''
	salida
		esta : ''El resultado de la búsqueda.
		        cierto si n esta en a; falso en caso contrario.''
	pre { a != nulo }
	pos { esta <=> (existe i:entero, 0 <= i && i < #a: (n = a[i])) }
fin especificación
