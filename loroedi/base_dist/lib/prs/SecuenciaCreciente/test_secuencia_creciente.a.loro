algoritmo para test_secuencia_creciente(alg: algoritmo para secuencia_creciente)
''Contiene código para probar el algoritmo pasado como argumento''
inicio
	// caso de frontera
	arr: []entero := crear[0] entero;  // arreglo vacio
	{ alg(arr) };

	// otros casos que deben ser ciertos:
	{ alg([1, 2, 3, 4, 5]) };
	{ alg([89, 101, 101, 500]) };

	// algunos casos que deben ser falsos:
	{ ! alg([5, 4]) };
	{ ! alg([101, 101, -240]) };
fin algoritmo